# Archived Slurm Logs

This directory contains historical slurm stdout/stderr logs for Hi eval jobs.

- `d25/`: logs from short-horizon evaluations (`eval.goal_offset_steps=25`).
- `d50/`: logs from medium-horizon evaluations (`eval.goal_offset_steps=50`).
