dataset:
  num_steps: ${eval:'${wm.history_size} + ${wm.high_level.waypoints.max_span}'}
  frameskip: 5
  name: tworoom
  keys_to_load:
    - pixels
    - action
    - proprio
  keys_to_cache:
    - action
    - proprio
