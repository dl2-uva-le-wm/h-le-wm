dataset:
  num_steps: ${eval:'${wm.history_size} + ${wm.high_level.waypoints.max_span}'}
  frameskip: 5
  name: pusht_expert_train
  keys_to_load:
    - pixels
    - action
    - proprio
    - state
  keys_to_cache:
    - action
    - proprio
    - state
