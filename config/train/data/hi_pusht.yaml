dataset:
  num_steps: ${eval:'${wm.history_size} + (${wm.k2} if ${wm.num_levels} == 3 else ${wm.k1})'}
  frameskip: 5
  name: pusht_expert_train
  keys_to_load:
    - pixels
    - action
    - proprio
    - state
  keys_to_cache:
    - action
    - proprio
    - state
