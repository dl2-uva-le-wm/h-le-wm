dataset:
  num_steps: ${eval:'${wm.history_size} + ${wm.k2}'}
  frameskip: 5
  name: pusht_expert_train
  keys_to_load:
    - pixels
    - action
    - proprio
    - state
  keys_to_cache:
    - action
    - proprio
    - state
