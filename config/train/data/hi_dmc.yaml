dataset:
  num_steps: ${eval:'${wm.history_size} + (${wm.k2} if ${wm.num_levels} == 3 else ${wm.k1})'}
  frameskip: 5
  name: reacher
  keys_to_load:
    - pixels
    - action
    - observation
  keys_to_cache:
    - action
    - observation
