defaults:
  - reacher
  - _self_

policy: hi_lewm

plan_config:
  # Short-horizon hierarchical planning (k1 in dataset-frame units).
  horizon: 1
  receding_horizon: 1
  action_block: 5

output:
  filename: hi_reacher_results.txt

